What will be the file position after each of these calls to lseek()?

lseek(fd, 0, SEEK_SET);
0

lseek(fd, 1000, SEEK_SET);
1000

lseek(fd, -100, SEEK_SET);
0

lseek(fd, 1000, SEEK_CUR);
1000

lseek(fd, -1000, SEEK_CUR);
0

// file is 10 000 bytes big
lseek(fd, 0, SEEK_END);
10 000
